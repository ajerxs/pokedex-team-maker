import { Component } from "react";

export default class PokemonList extends Component {
    render() {
        return(
            <h1>This should be a Pokemon list component eventually.</h1>
        )
    }
}