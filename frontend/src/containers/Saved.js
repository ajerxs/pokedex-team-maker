import { Component } from "react";

class Saved extends Component {
    render() {
        return(
            <div>This will be a separate nav that will display all saved teams.</div>
        )
    }
}

export default Saved;